# Markdown Editor

## To Run:

Open new terminal/command prompt in the folder directory.

Run commands:

### `npm i`

### `npm start`

Runs the app in the development mode on port 3000.

Open (http://localhost:3000) to view it in your browser.